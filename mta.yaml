_schema-version: 3.3.0
ID: NFAApprovers
description: A simple CAP project.
version: 1.0.0
modules:
- name: NFAApprovers-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: NFAApprovers-db
  - name: uaa_NFAApprovers
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: NFAApprovers-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: NFAApprovers-db
  parameters:
    buildpack: nodejs_buildpack
- name: NFAApprovers-destination-content
  type: com.sap.application.content
  requires:
  - name: NFAApprovers-destination-service
    parameters:
      content-target: true
  - name: NFAApprovers_html_repo_host
    parameters:
      service-key:
        name: NFAApprovers_html_repo_host-key
  - name: uaa_NFAApprovers
    parameters:
      service-key:
        name: uaa_NFAApprovers-key
  parameters:
    content:
      instance:
        destinations:
        - Name: NfaApproversAppRouter_NFAApprovers_html_repo_host
          ServiceInstanceName: NFAApprovers-html5-app-host-service
          ServiceKeyName: NFAApprovers_html_repo_host-key
          sap.cloud.service: NfaApproversAppRouter
        - Authentication: OAuth2UserTokenExchange
          Name: NfaApproversAppRouter_uaa_NFAApprovers
          ServiceInstanceName: NFAApprovers-xsuaa-service
          ServiceKeyName: uaa_NFAApprovers-key
          sap.cloud.service: NfaApproversAppRouter
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: NFAApprovers-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: NFAApprovers_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nfaapprovers.zip
      name: nfaapprovers
      target-path: resources/
- name: nfaapprovers
  type: html5
  path: app/nfaapprovers
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: NFAApprovers-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: NFAApprovers-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: NFAApprovers-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: NFAApprovers_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: NFAApprovers-html5-app-host-service
    service-plan: app-host
- name: uaa_NFAApprovers
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: NFAApprovers-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
